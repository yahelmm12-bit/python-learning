#Create a list that does the following: prints how many elements there are using len(); Prints the first three elements using slicing; Prints the last three elements using slicing; Prints [30, 40, 50] using slicing; And lastly, print the entire list using slicing, but without using all the numbers again
numbers = [10, 20, 30, 40, 50, 60, 70]
print(len(numbers))
print(numbers[:3])
print(numbers[-3:])
print(numbers[2:5])
print(numbers[:])
